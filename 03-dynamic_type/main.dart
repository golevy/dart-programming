// The `dynamic` type not only allows the variable's type to change dynamically during runtime but also permits the value to change.
void main() {
  dynamic age = 20;
  age = "Hello";
  print(age / 3);
}
